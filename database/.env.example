# Database credentials shared by docker-compose.yml and the backend.
# Copy this file to ".env" in the same folder; docker compose loads it
# automatically. The compose file also has built-in defaults, so the stack
# still runs even without this file.
DB_ROOT_PASSWORD=rootpass
DB_NAME=spotlight
DB_USER=spotlight
DB_PASSWORD=spotlightpass
